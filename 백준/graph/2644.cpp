#include <iostream>
#include <vector>

using namespace std;

int main(void) {
    int n;
    cin >> n;

    int x, y;
    cin >> x >> y;

    int m;
    cin >> m;

    for(int i = 0; i < m; i++) {
        int first, second;
        cin >> first >> second;
    }
}